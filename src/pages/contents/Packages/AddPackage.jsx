import React from "react";

import { API } from "../../../api/api";
import { useParams } from "react-router-dom";

function AddPackage({ refetch }) {
  const { courseId } = useParams();

  //  course_details_id,
  //   title,
  //   price,
  //   duration,
  //   intro_url,
  //   description,

  api: return <div>AddPackage</div>;
}

export default AddPackage;
